dueDate: 03/29/2025
latePenalties: [10, 20]
test:
  launch: sml
  grading:
    - oracle: self
      pattern: "(?<pts>[0-9]+)/(?<poss>[0-9]+)"
  timelimit: 10
build:
  command: echo N/A
  weight: 0
  instructorFiles:
    include: ["**/*", '*']
    exclude: ["**/insert.sml", "**/remove.sml"]
  studentFiles:
    include: ["**/insert.sml", "**/remove.sml"]
    exclude: [".git"]

  